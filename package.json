{
  "name": "remote-styles",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest",
    "clean": "rm -rf ./dist",
    "rollup": "rollup --config",
    "copy:pub": "cp ./package.pub.json  ./dist/packages-dist/remote-styles/package.json",
    "copy:pub:loader": "cp ./package.pub.loader.json ./dist/packages-dist/remote-styles/loader/package.json",
    "build": "npm run clean && npm run rollup && npm run copy:pub && npm run copy:pub:loader"
  },
  "keywords": [],
  "author": "",
  "license": "Apache 2.0",
  "dependencies": {
    "firebase": "^7.2.0"
  },
  "devDependencies": {
    "@babel/core": "^7.6.4",
    "@babel/preset-env": "^7.6.3",
    "@babel/preset-typescript": "^7.6.0",
    "babel-jest": "^24.9.0",
    "jest": "^24.9.0",
    "rollup": "^1.24.0",
    "rollup-plugin-typescript": "^1.0.1",
    "@types/jest": "^24.0.19",
    "typescript": "^3.6.4"
  }
}
